# OCR by Iron Minds
Currently shows the image processing and a neural network learning the XOR function.

## Usage
### Build
Run make to compile this project to bin/ocr.
Furthermore, there are clean and run rules to remove temporary files and run the program without any argument.

### Run
For the moment, only demos are available for the image segmentation and the neural network parts :

```sh
# Image part
bin/ocr img
# Network part
bin/ocr net
```
